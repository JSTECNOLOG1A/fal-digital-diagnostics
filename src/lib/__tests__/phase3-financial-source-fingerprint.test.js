import {it,expect} from 'vitest';import {readFileSync} from 'node:fs';it('prepare fingerprint includes material source outputs',()=>{const s=readFileSync('base44/functions/prepareFinancialAnalysisDataset/entry.ts','utf8');for(const key of ['source_processing_run_id','source_snapshot_id','source_output_checksum','mapping_checksum','registry_hash','formula_version'])expect(s).toContain(key);});