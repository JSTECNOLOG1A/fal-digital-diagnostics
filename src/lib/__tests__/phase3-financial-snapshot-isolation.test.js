import {it,expect} from 'vitest';import {readFileSync} from 'node:fs';it('snapshot is fail-closed and run scoped',()=>{const s=readFileSync('base44/functions/createFinancialProcessingSnapshot/entry.ts','utf8');expect(s).toContain('SNAPSHOT_RUN_OUTPUTS_REQUIRED');expect(s).toContain('processing_run_id');expect(s).toContain('publication_status');expect(s).not.toContain('Fallback: todos os outputs do diagnóstico');});