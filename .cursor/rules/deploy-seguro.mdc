---
description: Entrega segura Método FAL — commit Git, push e deploy VPS com Traefik
alwaysApply: false
---

# Entrega segura (Git + Deploy)

Ao usuário pedir salvar, commit, push ou deploy deste projeto, seguir `docs/deploy/UPDATE.md` e o prompt em `docs/deploy/PROMPT-AGENTE-DEPLOY.md`.

## Ordem obrigatória
1. Testar/alterar
2. Commit (se pedido)
3. Push (se pedido)
4. Sync VPS `/var/www/html/metodofal`
5. `docker compose -f docker-compose.traefik.yml --env-file .env.production up -d --build`
6. Smoke: containers healthy + `/api/v1/health` + feature

## Proibido
- Commitar `.env` / `.env.production` / secrets
- Force push em main
- Sobrescrever `.env.production` no VPS
- Deploy de working tree não commitada
- Apagar volumes de produção sem pedido explícito

## Deploy
- Responder em português
- Confirmar hash do commit deployado
- Aguardar `fal-prod-web` / `fal-prod-api` healthy antes de declarar sucesso
- Domínio: https://metodofal.clarityib.com.br
