import { createClientFromRequest } from 'npm:@base44/sdk@0.8.38';
const VALID_APP_ROLES=new Set(['hq_admin','tenant_admin','consultant','client_viewer']);const WRITE_ROLES=new Set(['hq_admin','tenant_admin','consultant']);function resolveAppRole(user){if(!user)return null;if(VALID_APP_ROLES.has(user.app_role))return user.app_role;return user.role==='admin'?'hq_admin':null;}function assertCanWrite(effectiveRole){if(!WRITE_ROLES.has(effectiveRole))throw Object.assign(new Error('Forbidden: write permission required'),{status:403});}async function readAllById(entity,query){const rows=[];let cursor=null;while(true){const page=await entity.filter(cursor?{...query,id:{$gt:cursor}}:query,'id',500);if(!page.length)break;rows.push(...page);if(page.length<500)break;cursor=page[page.length-1].id;}return rows;}
Deno.serve(async(req)=>{try{const base44=createClientFromRequest(req);const user=await base44.auth.me();if(!user)return Response.json({error:'Unauthorized'},{status:401});const effectiveRole=resolveAppRole(user);assertCanWrite(effectiveRole);const {report_version_id}=await req.json();const version=await base44.asServiceRole.entities.AssessmentReportVersion.get(report_version_id);if(!version)return Response.json({error:'Report version not found'},{status:404});if(effectiveRole!=='hq_admin'&&version.tenant_id!==user.tenant_id)return Response.json({error:'Forbidden: tenant mismatch'},{status:403});if(!['generated','active'].includes(version.status))return Response.json({error:'Only generated or active reports can be official'},{status:409});const versions=await readAllById(base44.asServiceRole.entities.AssessmentReportVersion,{tenant_id:version.tenant_id,assessment_id:version.assessment_id,report_type:version.report_type});const previous=versions.filter(item=>item.mark_as_official);try{await base44.asServiceRole.entities.AssessmentReportVersion.update(version.id,{mark_as_official:true,status:'active'});for(const item of previous.filter(item=>item.id!==version.id))await base44.asServiceRole.entities.AssessmentReportVersion.update(item.id,{mark_as_official:false});const confirmed=await readAllById(base44.asServiceRole.entities.AssessmentReportVersion,{tenant_id:version.tenant_id,assessment_id:version.assessment_id,report_type:version.report_type});if(confirmed.filter(item=>item.mark_as_official).length!==1)throw new Error('OFFICIAL_REPORT_UNIQUENESS_FAILED');return Response.json({report_version:confirmed.find(item=>item.mark_as_official)});}catch(error){await base44.asServiceRole.entities.AssessmentReportVersion.update(version.id,{mark_as_official:false});for(const item of previous)await base44.asServiceRole.entities.AssessmentReportVersion.update(item.id,{mark_as_official:true});throw error;}}catch(error){return Response.json({error:error.message},{status:error.status||500});}});