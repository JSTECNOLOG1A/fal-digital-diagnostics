# Owner — migrations / seed
DATABASE_URL_OWNER=postgresql://fal:fal_dev_only_change_me@localhost:5433/fal?schema=public

# Runtime — role fal_app SEM BYPASSRLS (ISO least privilege)
DATABASE_URL=postgresql://fal_app:fal_app_dev_only_change_me@localhost:5433/fal?schema=public

# API
API_PORT=3001
API_PREFIX=api/v1
NODE_ENV=development
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Auth (trocar em produção — mínimo 32 chars)
JWT_ACCESS_SECRET=fal-dev-access-secret-change-me-32c
JWT_REFRESH_SECRET=fal-dev-refresh-secret-change-me-32
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=7d

# Redis / filas
REDIS_URL=redis://localhost:6381

# Object storage (S3 / MinIO)
S3_ENDPOINT=http://localhost:9100
S3_REGION=us-east-1
S3_ACCESS_KEY=fal
S3_SECRET_KEY=fal_minio_dev_secret
S3_BUCKET=fal

# Criptografia de credenciais de integração (64 hex = 32 bytes)
CREDENTIALS_ENCRYPTION_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef

# Seed (dev only)
SEED_HQ_EMAIL=admin@fal.local
SEED_HQ_PASSWORD=FalTest123!
SEED_TENANT_SLUG=demo
SEED_TENANT_NAME=Tenant Demo Local
